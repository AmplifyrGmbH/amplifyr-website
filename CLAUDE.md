# Amplifyr Website — Projektnotizen

## Lokaler Dev-Server starten

```bash
php -S localhost:8080 dev-router.php
```

`dev-router.php` mappt Clean URLs (`/webdesign` → `webdesign.html`), da `.htaccess` mit PHPs eingebautem Server nicht funktioniert.
